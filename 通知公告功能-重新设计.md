# 通知公告功能 - 重新设计方案

## 📋 问题分析

### 当前存在的问题

1. **学生端看不到公告**: 学生端首页无法看到管理员和学校发布的公告
2. **学校端无法编辑删除**: 学校端发布公告后不能编辑和删除自己的公告
3. **目标受众过于复杂**: 学校发布公告还要区分教师和学生，太麻烦
4. **教师端缺失**: 教师端没有公告功能，暂时不需要开发
5. **附件功能**: 不需要附件功能

### 数据库表结构

```sql
CREATE TABLE public.sys_notice (
  notice_id varchar(64) NOT NULL,
  notice_title varchar(200) NOT NULL,
  notice_type varchar(10) NOT NULL,
  notice_content text NULL,
  status int2 NULL DEFAULT 0,
  is_top int2 NULL DEFAULT 0,
  view_count int4 NULL DEFAULT 0,
  attachments text NULL,  -- 不再使用
  create_by varchar(64) NULL,
  create_time timestamp NULL DEFAULT CURRENT_TIMESTAMP,
  update_time timestamp NULL DEFAULT CURRENT_TIMESTAMP,
  publish_time timestamp NULL,
  publisher_type varchar(255) NULL,  -- 'admin' 或 'school'
  publisher_id varchar(255) NULL,    -- 学校ID（学校发布时）
  target_audience varchar(255) NULL, -- 'all' 或 'student'
  CONSTRAINT sys_notice_pkey PRIMARY KEY (notice_id)
);
```

---

## 🎯 重新设计方案

### 核心原则

1. **简化权限**: 管理员发布全局可见，学校发布本校可见
2. **简化受众**: 学校发布的公告本校学生和教师都能看（不区分）
3. **明确权限**: 学校只能编辑删除自己发布的公告
4. **移除附件**: 不再支持附件功能

### 权限矩阵

| 角色 | 可查看 | 可新增 | 可编辑 | 可删除 | 可置顶 |
|------|--------|--------|--------|--------|--------|
| 管理员 | 全部 | ✅ | 全部 | 全部 | ✅ |
| 学校 | 管理员+本校 | ✅ | 本校 | 本校 | ❌ |
| 学生 | 管理员+本校 | ❌ | ❌ | ❌ | ❌ |
| 教师 | 暂不开发 | ❌ | ❌ | ❌ | ❌ |

### 字段使用规则

| 字段 | 管理员发布 | 学校发布 | 说明 |
|------|-----------|----------|------|
| publisher_type | 'admin' | 'school' | 发布者类型 |
| publisher_id | NULL | 学校ID | 学校发布时存储学校ID |
| target_audience | 'all' 或 'student' | 'student' | 管理员可选，学校固定为student |
| create_by | 管理员用户ID | 学校用户ID | 创建人ID |

---

## 📝 接口设计

### 1. 管理员端接口

#### 1.1 获取公告列表

**接口**: `GET /api/notice/list`

**权限**: 管理员

**查询参数**:
```typescript
{
  pageNum: number
  pageSize: number
  noticeTitle?: string
  noticeType?: string  // 1=通知, 2=公告, 3=政策, 4=新闻
  status?: number      // 0=草稿, 1=已发布
}
```

**SQL**:
```sql
SELECT 
  notice_id AS "noticeId",
  notice_title AS "noticeTitle",
  notice_type AS "noticeType",
  notice_content AS "noticeContent",
  status,
  is_top AS "isTop",
  view_count AS "viewCount",
  create_by AS "createBy",
  create_time AS "createTime",
  update_time AS "updateTime",
  publish_time AS "publishTime",
  publisher_type AS "publisherType",
  publisher_id AS "publisherId",
  target_audience AS "targetAudience"
FROM sys_notice
WHERE 1=1
  AND (#{noticeTitle} IS NULL OR notice_title LIKE CONCAT('%', #{noticeTitle}, '%'))
  AND (#{noticeType} IS NULL OR notice_type = #{noticeType})
  AND (#{status} IS NULL OR status = #{status})
ORDER BY is_top DESC, publish_time DESC, create_time DESC
LIMIT #{pageSize} OFFSET (#{pageNum} - 1) * #{pageSize};
```

#### 1.2 新增公告

**接口**: `POST /api/notice/add`

**权限**: 管理员

**请求体**:
```typescript
{
  noticeTitle: string
  noticeType: string
  noticeContent: string
  targetAudience: string  // 'all' 或 'student'
  isTop: number           // 0 或 1
  status: number          // 0=草稿, 1=已发布
}
```

**SQL**:
```sql
INSERT INTO sys_notice (
  notice_id,
  notice_title,
  notice_type,
  notice_content,
  status,
  is_top,
  view_count,
  create_by,
  create_time,
  update_time,
  publish_time,
  publisher_type,
  publisher_id,
  target_audience
) VALUES (
  #{noticeId},
  #{noticeTitle},
  #{noticeType},
  #{noticeContent},
  #{status},
  #{isTop},
  0,
  #{createBy},
  CURRENT_TIMESTAMP,
  CURRENT_TIMESTAMP,
  CASE WHEN #{status} = 1 THEN CURRENT_TIMESTAMP ELSE NULL END,
  'admin',
  NULL,
  #{targetAudience}
);
```

#### 1.3 更新公告

**接口**: `PUT /api/notice/update`

**权限**: 管理员

**请求体**:
```typescript
{
  noticeId: string
  noticeTitle: string
  noticeType: string
  noticeContent: string
  targetAudience: string
  isTop: number
  status: number
}
```

**SQL**:
```sql
UPDATE sys_notice
SET 
  notice_title = #{noticeTitle},
  notice_type = #{noticeType},
  notice_content = #{noticeContent},
  target_audience = #{targetAudience},
  is_top = #{isTop},
  status = #{status},
  update_time = CURRENT_TIMESTAMP,
  publish_time = CASE 
    WHEN #{status} = 1 AND publish_time IS NULL 
    THEN CURRENT_TIMESTAMP 
    ELSE publish_time 
  END
WHERE notice_id = #{noticeId};
```

#### 1.4 删除公告

**接口**: `DELETE /api/notice/delete/{noticeId}`

**权限**: 管理员

**SQL**:
```sql
DELETE FROM sys_notice WHERE notice_id = #{noticeId};
```

#### 1.5 发布/停用/置顶

同现有接口，不变。

---

### 2. 学校端接口

#### 2.1 获取公告列表

**接口**: `GET /api/school/notice/list`

**权限**: 学校

**查询参数**:
```typescript
{
  pageNum: number
  pageSize: number
  noticeTitle?: string
  noticeType?: string
  source?: string  // 'admin'=管理员发布, 'school'=本校发布
}
```

**SQL**:
```sql
SELECT 
  n.notice_id AS "noticeId",
  n.notice_title AS "noticeTitle",
  n.notice_type AS "noticeType",
  n.notice_content AS "noticeContent",
  n.status,
  n.is_top AS "isTop",
  n.view_count AS "viewCount",
  n.create_by AS "createBy",
  n.create_time AS "createTime",
  n.update_time AS "updateTime",
  n.publish_time AS "publishTime",
  n.publisher_type AS "publisherType",
  n.publisher_id AS "publisherId",
  n.target_audience AS "targetAudience"
FROM sys_notice n
WHERE 1=1
  -- 只显示已发布的管理员公告，或本校的所有公告（包括草稿）
  AND (
    (n.publisher_type = 'admin' AND n.status = 1)
    OR (n.publisher_type = 'school' AND n.publisher_id = #{schoolId})
  )
  -- 标题筛选
  AND (#{noticeTitle} IS NULL OR n.notice_title LIKE CONCAT('%', #{noticeTitle}, '%'))
  -- 类型筛选
  AND (#{noticeType} IS NULL OR n.notice_type = #{noticeType})
  -- 来源筛选
  AND (
    #{source} IS NULL 
    OR (#{source} = 'admin' AND n.publisher_type = 'admin')
    OR (#{source} = 'school' AND n.publisher_type = 'school' AND n.publisher_id = #{schoolId})
  )
ORDER BY n.is_top DESC, n.publish_time DESC, n.create_time DESC
LIMIT #{pageSize} OFFSET (#{pageNum} - 1) * #{pageSize};
```

**业务逻辑**:
- 学校ID从登录用户的 `owner_id` 获取
- 管理员公告只显示已发布的
- 本校公告显示所有状态（包括草稿）

#### 2.2 新增公告

**接口**: `POST /api/school/notice/add`

**权限**: 学校

**请求体**:
```typescript
{
  noticeTitle: string
  noticeType: string
  noticeContent: string
  status: number  // 0=草稿, 1=已发布
}
```

**SQL**:
```sql
INSERT INTO sys_notice (
  notice_id,
  notice_title,
  notice_type,
  notice_content,
  status,
  is_top,
  view_count,
  create_by,
  create_time,
  update_time,
  publish_time,
  publisher_type,
  publisher_id,
  target_audience
) VALUES (
  #{noticeId},
  #{noticeTitle},
  #{noticeType},
  #{noticeContent},
  #{status},
  0,  -- 学校发布的公告不能置顶
  0,
  #{createBy},
  CURRENT_TIMESTAMP,
  CURRENT_TIMESTAMP,
  CASE WHEN #{status} = 1 THEN CURRENT_TIMESTAMP ELSE NULL END,
  'school',
  #{schoolId},
  'student'  -- 固定为student，本校学生和教师都能看
);
```

#### 2.3 更新公告

**接口**: `PUT /api/school/notice/update`

**权限**: 学校（只能更新本校发布的）

**请求体**:
```typescript
{
  noticeId: string
  noticeTitle: string
  noticeType: string
  noticeContent: string
  status: number
}
```

**SQL**:
```sql
UPDATE sys_notice
SET 
  notice_title = #{noticeTitle},
  notice_type = #{noticeType},
  notice_content = #{noticeContent},
  status = #{status},
  update_time = CURRENT_TIMESTAMP,
  publish_time = CASE 
    WHEN #{status} = 1 AND publish_time IS NULL 
    THEN CURRENT_TIMESTAMP 
    ELSE publish_time 
  END
WHERE notice_id = #{noticeId}
  AND publisher_type = 'school'
  AND publisher_id = #{schoolId};  -- 权限校验
```

**返回**: 如果影响行数为0，返回错误"无权限操作"

#### 2.4 删除公告

**接口**: `DELETE /api/school/notice/delete/{noticeId}`

**权限**: 学校（只能删除本校发布的）

**SQL**:
```sql
DELETE FROM sys_notice 
WHERE notice_id = #{noticeId}
  AND publisher_type = 'school'
  AND publisher_id = #{schoolId};  -- 权限校验
```

#### 2.5 发布公告

**接口**: `PUT /api/school/notice/publish/{noticeId}`

**权限**: 学校（只能发布本校的草稿）

**SQL**:
```sql
UPDATE sys_notice
SET 
  status = 1,
  publish_time = CURRENT_TIMESTAMP,
  update_time = CURRENT_TIMESTAMP
WHERE notice_id = #{noticeId}
  AND publisher_type = 'school'
  AND publisher_id = #{schoolId}
  AND status = 0;
```

#### 2.6 停用公告

**接口**: `PUT /api/school/notice/unpublish/{noticeId}`

**权限**: 学校（只能停用本校的已发布公告）

**SQL**:
```sql
UPDATE sys_notice
SET 
  status = 0,
  update_time = CURRENT_TIMESTAMP
WHERE notice_id = #{noticeId}
  AND publisher_type = 'school'
  AND publisher_id = #{schoolId}
  AND status = 1;
```

---

### 3. 学生端接口

#### 3.1 获取首页公告列表（最新5条）

**接口**: `GET /api/mobile/home/notices`

**权限**: 学生

**SQL**:
```sql
SELECT 
  n.notice_id AS "noticeId",
  n.notice_title AS "noticeTitle",
  n.notice_type AS "noticeType",
  n.publish_time AS "publishTime",
  n.publisher_type AS "publisherType",
  CASE 
    WHEN n.publisher_type = 'admin' THEN '省教育厅'
    WHEN n.publisher_type = 'school' THEN s.name
    ELSE '未知'
  END AS "publisherName",
  n.is_top AS "isTop",
  n.view_count AS "viewCount"
FROM sys_notice n
LEFT JOIN sys_school s ON n.publisher_id = s.id AND n.publisher_type = 'school'
WHERE n.status = 1  -- 只显示已发布的
  AND (
    -- 管理员发布的面向学生的公告
    (n.publisher_type = 'admin' AND n.target_audience IN ('all', 'student'))
    -- 本校发布的公告
    OR (n.publisher_type = 'school' AND n.publisher_id = #{studentSchoolId})
  )
ORDER BY n.is_top DESC, n.publish_time DESC
LIMIT 5;
```

**业务逻辑**:
- 学生学校ID从 `biz_student.school_id` 获取
- 只显示已发布的公告
- 管理员公告：target_audience 为 'all' 或 'student'
- 学校公告：本校发布的所有公告

#### 3.2 获取公告详情

**接口**: `GET /api/mobile/notice/{noticeId}`

**权限**: 学生

**SQL**:
```sql
-- 查询详情
SELECT 
  n.notice_id AS "noticeId",
  n.notice_title AS "noticeTitle",
  n.notice_type AS "noticeType",
  n.notice_content AS "noticeContent",
  n.publish_time AS "publishTime",
  n.publisher_type AS "publisherType",
  CASE 
    WHEN n.publisher_type = 'admin' THEN '省教育厅'
    WHEN n.publisher_type = 'school' THEN s.name
    ELSE '未知'
  END AS "publisherName",
  n.is_top AS "isTop",
  n.view_count AS "viewCount"
FROM sys_notice n
LEFT JOIN sys_school s ON n.publisher_id = s.id AND n.publisher_type = 'school'
WHERE n.notice_id = #{noticeId}
  AND n.status = 1
  AND (
    (n.publisher_type = 'admin' AND n.target_audience IN ('all', 'student'))
    OR (n.publisher_type = 'school' AND n.publisher_id = #{studentSchoolId})
  );

-- 更新浏览次数
UPDATE sys_notice
SET view_count = view_count + 1
WHERE notice_id = #{noticeId};
```

#### 3.3 获取公告列表（完整列表）

**接口**: `GET /api/mobile/notices`

**权限**: 学生

**查询参数**:
```typescript
{
  pageNum: number
  pageSize: number
  noticeType?: string
  publisherType?: string  // 'admin' 或 'school'
}
```

**SQL**:
```sql
SELECT 
  n.notice_id AS "noticeId",
  n.notice_title AS "noticeTitle",
  n.notice_type AS "noticeType",
  n.publish_time AS "publishTime",
  n.publisher_type AS "publisherType",
  CASE 
    WHEN n.publisher_type = 'admin' THEN '省教育厅'
    WHEN n.publisher_type = 'school' THEN s.name
    ELSE '未知'
  END AS "publisherName",
  n.is_top AS "isTop",
  n.view_count AS "viewCount"
FROM sys_notice n
LEFT JOIN sys_school s ON n.publisher_id = s.id AND n.publisher_type = 'school'
WHERE n.status = 1
  AND (
    (n.publisher_type = 'admin' AND n.target_audience IN ('all', 'student'))
    OR (n.publisher_type = 'school' AND n.publisher_id = #{studentSchoolId})
  )
  AND (#{noticeType} IS NULL OR n.notice_type = #{noticeType})
  AND (#{publisherType} IS NULL OR n.publisher_type = #{publisherType})
ORDER BY n.is_top DESC, n.publish_time DESC
LIMIT #{pageSize} OFFSET (#{pageNum} - 1) * #{pageSize};
```

---

## 🔧 前端修改

### 1. 管理员端 (src/views/admin/notice/index.vue)

**需要修改的地方**:

1. 表单添加 `targetAudience` 字段：
```vue
<el-form-item label="目标受众" prop="targetAudience">
  <el-radio-group v-model="formData.targetAudience">
    <el-radio value="all">全部用户</el-radio>
    <el-radio value="student">学生</el-radio>
  </el-radio-group>
</el-form-item>
```

2. 列表显示目标受众：
```vue
<el-table-column prop="targetAudience" label="目标受众" width="100" align="center">
  <template #default="{ row }">
    <el-tag :type="row.targetAudience === 'all' ? 'success' : 'primary'" size="small">
      {{ row.targetAudience === 'all' ? '全部' : '学生' }}
    </el-tag>
  </template>
</el-table-column>
```

### 2. 学校端 (src/views/school/notice/index.vue)

**需要修改的地方**:

1. **移除目标受众选择**（学校发布固定为student）
2. **修改权限判断逻辑**：
```typescript
// 判断是否为管理员公告
const isAdminNotice = (row: SchoolNoticeItem) => {
  return row.publisherType === 'admin'
}
```

3. **修改操作按钮显示逻辑**：
```vue
<template v-if="!isAdminNotice(row)">
  <el-button link type="primary" size="small" @click="handleEdit(row)">编辑</el-button>
  <el-button link type="danger" size="small" @click="handleDelete(row)">删除</el-button>
  <!-- 其他操作 -->
</template>
<el-tag v-else type="info" size="small">仅可查看</el-tag>
```

### 3. 学生端 (src/views/mobile/home/index.vue)

**当前问题**: 学生端首页看不到公告

**原因分析**: 
- 后端接口可能没有正确实现
- 或者前端调用的接口路径不对

**需要确认**:
1. 后端是否实现了 `/api/mobile/home/notices` 接口
2. 接口是否正确返回数据
3. 前端是否正确调用

---

## 📊 数据流转图

```
管理员发布公告
  ├─ publisher_type = 'admin'
  ├─ publisher_id = NULL
  ├─ target_audience = 'all' 或 'student'
  └─ 可见范围：
      ├─ all: 所有学校、所有学生
      └─ student: 所有学生

学校发布公告
  ├─ publisher_type = 'school'
  ├─ publisher_id = 学校ID
  ├─ target_audience = 'student' (固定)
  └─ 可见范围：
      ├─ 本校学生
      └─ 本校教师（暂不开发）
```

---

## ✅ 测试用例

### 管理员端测试

1. **新增公告**
   - 保存草稿 → 检查 status=0
   - 直接发布 → 检查 status=1, publish_time 不为空
   - 选择目标受众 → 检查 target_audience 正确

2. **编辑公告**
   - 编辑管理员公告 → 成功
   - 编辑学校公告 → 成功（管理员可以编辑所有）

3. **删除公告**
   - 删除任意公告 → 成功

### 学校端测试

1. **查看列表**
   - 能看到管理员已发布的公告
   - 能看到本校所有公告（包括草稿）
   - 看不到其他学校的公告

2. **新增公告**
   - 保存草稿 → 检查 publisher_type='school', publisher_id=学校ID
   - 直接发布 → 检查 status=1

3. **编辑公告**
   - 编辑本校公告 → 成功
   - 编辑管理员公告 → 失败（按钮不显示）

4. **删除公告**
   - 删除本校公告 → 成功
   - 删除管理员公告 → 失败（按钮不显示）

### 学生端测试

1. **首页公告**
   - 能看到管理员发布的公告（target_audience='all' 或 'student'）
   - 能看到本校发布的公告
   - 看不到其他学校的公告
   - 最多显示5条

2. **公告详情**
   - 点击查看详情 → 显示完整内容
   - 浏览次数 +1

3. **公告列表**
   - 分页加载
   - 按类型筛选
   - 按发布者筛选

---

## 🚀 实施步骤

### 第一步：后端修改

1. **管理员端接口**
   - 修改新增接口：添加 `targetAudience` 参数
   - 修改更新接口：添加 `targetAudience` 参数
   - 修改列表接口：返回 `publisherType`, `publisherId`, `targetAudience`

2. **学校端接口**
   - 修改列表查询SQL：使用 `publisher_type` 和 `publisher_id` 判断
   - 修改新增接口：设置 `publisher_type='school'`, `publisher_id=学校ID`
   - 修改更新/删除接口：添加权限校验 `AND publisher_id = #{schoolId}`

3. **学生端接口**
   - 实现 `/api/mobile/home/notices` 接口
   - 实现 `/api/mobile/notice/{noticeId}` 接口
   - 实现 `/api/mobile/notices` 接口

### 第二步：前端修改

1. **管理员端**
   - 添加目标受众选择
   - 列表显示目标受众

2. **学校端**
   - 移除目标受众选择
   - 修改权限判断逻辑
   - 修改操作按钮显示

3. **学生端**
   - 确认接口调用正确
   - 测试数据显示

### 第三步：测试验证

1. 管理员端测试
2. 学校端测试
3. 学生端测试

---

## 📝 总结

### 核心改动

1. **简化权限**: 使用 `publisher_type` 和 `publisher_id` 明确发布者
2. **简化受众**: 学校发布的公告本校都能看，不区分教师学生
3. **修复权限**: 学校可以编辑删除自己的公告
4. **修复显示**: 学生端能正确看到管理员和本校公告

### 数据库字段使用

| 字段 | 管理员 | 学校 | 说明 |
|------|--------|------|------|
| publisher_type | 'admin' | 'school' | 发布者类型 |
| publisher_id | NULL | 学校ID | 发布者ID |
| target_audience | 'all'/'student' | 'student' | 目标受众 |
| create_by | 用户ID | 用户ID | 创建人 |

### 权限控制

- **管理员**: 可以操作所有公告
- **学校**: 只能操作 `publisher_type='school' AND publisher_id=学校ID` 的公告
- **学生**: 只能查看，不能操作

---

**文档版本**: v2.0  
**创建日期**: 2026年2月21日  
**适用范围**: 通知公告功能重新设计
